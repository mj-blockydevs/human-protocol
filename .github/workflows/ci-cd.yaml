name: CI/CD

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Change directory
        run: cd packages/apps/fortune/recording-oracle

      - name: Install dependencies
        run: yarn

      - name: Build
        run: yarn build

      - name: Start production application
        run: yarn start:prod
